[project]
name = "Lab2 Oscar Rivera"
version = "0.1.0"
description = "Minimal Python Playwright automation project"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "playwright>=1.54.0",
]



[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"